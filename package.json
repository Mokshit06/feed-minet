{
  "name": "minet",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "server": "yarn workspace server dev",
    "web": "yarn workspace web dev",
    "dev": "concurrently \"yarn server\" \"yarn web\""
  },
  "devDependencies": {
    "concurrently": "^6.2.1"
  }
}
